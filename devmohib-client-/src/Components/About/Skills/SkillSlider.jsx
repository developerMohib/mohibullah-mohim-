
import { Backend_skill, Design, Frontend_skill, Skill_data } from "../../../utils/skilldata";

const SkillSlider = () => {
  const mySkill = [...Skill_data, ...Frontend_skill, ...Backend_skill, ...Design];

  return (
    <div>

    </div>
  );
};

export default SkillSlider;
